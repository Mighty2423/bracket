version: "3.8"
services:
  frontend:
    build: .
    ports:
      - "80:80"
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: bracket
      MYSQL_USER: admin
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"